**Задание 1. Анализ HTTP-запросов. Часть 1**

**URL запроса:**
https://en.wikipedia.org/wiki/HTTP

**Метод запроса:**
GET
Почему используется GET: Метод GET применяется для получения данных с сервера. При открытии веб-страницы браузер запрашивает HTML-документ, не изменяя данные на сервере.

**Статус ответ:**
200 OK
**Значение:**
Запрос успешно обработан сервером, страница найдена и корректно передана клиенту.

В запросе присутствуют следующие основные заголовки:

**Заголовки запроса:**
- `User-Agent` — содержит информацию о браузере и операционной системе пользователя  
- `Accept` — указывает, какие типы данных может принять клиент  
- `Accept-Language` — задаёт предпочтительный язык содержимого  
- `Accept-Encoding` — определяет способы сжатия данных  
- `Cookie` — используется для хранения сессионных данных  
- `Referer` — указывает страницу, с которой был выполнен переход

**Заголовки ответа:**
- `Content-Type` — определяет тип возвращаемых данных (HTML)  
- `Content-Encoding` — указывает, что данные передаются в сжатом виде  
- `Content-Length` — размер тела ответа  
- `Cache-Control` — управляет кэшированием данных  
- `Server` — информация о сервере

**Тело запроса и ответа:**
Тело запроса отсутствует, так как используется метод GET.  
Тело ответа содержит HTML-код веб-страницы, который используется браузером для отображения содержимого статьи.

<img width="558" height="171" alt="image" src="https://github.com/user-attachments/assets/2723ab7d-c5de-4ab6-962b-115d0ebc8abc" />


**Адрес:**
https://en.wikipedia.org/wiki/HTTPdsfdfs

**Статус ответа:**
404 Not Found

**Почему:**
Такой страницы не существует на сервере, поэтому сервер возвращает ошибку «ресурс не найден».
<img width="668" height="169" alt="image" src="https://github.com/user-attachments/assets/9f0c5376-13d5-4927-a235-07747cce61dc" />

**Задание 2. Анализ HTTP-запросов. Часть 2**

**URL запрос:**

https://en.wikipedia.org/w/index.php  

Данный URL используется для обработки поисковых запросов на сайте Wikipedia.


> **Метод запроса:**

`GET`  

Метод GET используется для получения данных с сервера.  
Поиск не изменяет данные на сервере, поэтому применяется данный метод.


> **Query Parameters:**

При выполнении поискового запроса передаются следующие параметры:

- `search=browser` — ключевое слово для поиска  
- `title=Special:Search` — страница обработки поискового запроса  
- `fulltext=Search` — тип выполняемого поиска  

Параметры передаются в URL и используются сервером для формирования результатов поиска.

<img width="1210" height="172" alt="image" src="https://github.com/user-attachments/assets/3b2c3bf2-2d45-4a33-b8a2-46fbd9cfd8ef" />

**Задание 3. Анализ HTTP-запросов. Часть 3**

Пример: GitHub
**URL:**

https://github.com

**Метод:**

GET

**Статус:**

200 OK

**Дополнительные запросы:**

JavaScript (React)
CSS
API-запросы (XHR / Fetch)

**Вывод:**
Современные сайты активно используют API и динамическую подгрузку данных.

**Задание 4. Составление HTTP-запросов**

1. **GET-запрос с User-Agent:**
GET / HTTP/1.1
Host: sandbox.usm.com
User-Agent: Peicov Valeri

**Что такое User-Agent:**

User-Agent — это HTTP-заголовок, который сообщает серверу:
тип браузера
операционную систему
информацию о клиенте

Используется для:
адаптации контента
аналитики
логирования запросов

2. **POST-запрос:**
POST /cars HTTP/1.1
Host: sandbox.usm.com
Content-Type: application/x-www-form-urlencoded
make=Toyota&model=Corolla&year=2020

**POST используется для создания новых данных на сервере.**

**3. Другие HTTP-методы:**
Метод	Назначение
GET -	Получение данных
POST - Создание данных
PUT -	Полное обновление ресурса
PATCH -	Частичное обновление
DELETE - Удаление
HEAD -	Только заголовки
OPTIONS -	Поддерживаемые методы

4. **PUT-запрос:**
PUT /cars/1 HTTP/1.1
Host: sandbox.usm.com
User-Agent: Peicov Valeri
Content-Type: application/json

{
  "make": "Toyota",
  "model": "Corolla",
  "year": 2021
}

**Разница между PUT и PATCH:**

PUT — полностью заменяет ресурс
PATCH — изменяет только указанные поля

5. **Пример ответа сервера:**
HTTP/1.1 201 Created
Content-Type: application/json

{
  "id": 5,
  "make": "Toyota",
  "model": "Corolla",
  "year": 2020,
  "status": "created"
}

6. **Возможные HTTP-коды ответа:**
Код	Ситуация
200	Успешный запрос
201	Ресурс создан
400	Ошибка в данных
401	Неавторизован
403	Запрещён доступ
404	Ресурс не найден
500	Ошибка сервера



